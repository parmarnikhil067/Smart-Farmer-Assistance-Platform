function ActivityCard({ text }) {

    return (

        <
        div className = "activity-card" >

        <
        p > { text } <
        /p>

        <
        /div>
    );
}

export default ActivityCard;